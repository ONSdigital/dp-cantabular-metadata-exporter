---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.16

inputs:
  - name: dp-cantabular-metadata-exporter

run:
  path: dp-cantabular-metadata-exporter/ci/scripts/component.sh
