---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-cantabular-metadata-exporter

run:
  path: dp-cantabular-metadata-exporter/ci/scripts/lint.sh
